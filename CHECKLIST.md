# Чек-лист миграции (Migration Checklist)

Этот чек-лист предназначен для контроля процесса перехода на Python/aiohttp стек с использованием микросервисной архитектуры.

## 🛠 1. Настройка окружения и Инфраструктура
- [ ] Создан `requirements.txt` / `pyproject.toml`.
    - [ ] `aiohttp` (web server & http client)
    - [ ] `uvloop` (event loop performance)
    - [ ] `asyncpg` (PostgreSQL driver)
    - [ ] `aiosqlite` (SQLite driver)
    - [ ] `sqlalchemy` (Core для SQL генерации)
    - [ ] `ollama` (AI Client или просто HTTP запросы)
    - [ ] `apscheduler` (Scheduler)
    - [ ] **Не подключать** `crawl4ai` как библиотеку (используем внешний сервис).
- [ ] Настроен `docker-compose.yml`:
    - [ ] Сервис `backend` (Python).
    - [ ] Сервис `db` (PostgreSQL).
    - [ ] Сервис `ollama` (AI Engine).
    - [ ] Сервис `crawl4ai` (External Scraper Service, image: `unclecode/crawl4ai:latest` или аналог).

## 🗄 2. База Данных
- [ ] Реализован `DatabaseManager` класс:
    - [ ] Поддержка DSN строки подключения.
    - [ ] Определение типа БД (sqlite/postgres).
    - [ ] Методы `connect()`, `disconnect()`, `execute()`, `fetch_all()`.
- [ ] Миграция схемы:
    - [ ] SQL скрипт создания таблиц.
    - [ ] Скрипт инициализации.

## 🌐 3. Backend API (aiohttp)
- [ ] Настройка `aiohttp.web.Application`.
- [ ] Middlewares (CORS, Auth).
- [ ] Эндпоинты (Auth, Scouts CRUD, Chat).

## 🤖 4. Интеграция внешних сервисов
- [ ] **Ollama Service**:
    - [ ] Асинхронный HTTP клиент для запросов к `http://ollama:11434`.
    - [ ] Реализация системных промптов.
- [ ] **Crawl4AI Service**:
    - [ ] Реализация HTTP клиента (`ScraperClient`) для общения с сервисом.
    - [ ] Метод `scrape(url)` -> отправляет POST запрос на сервис скрапера -> получает JSON с markdown.
    - [ ] Обработка ошибок (сервис недоступен, таймаут, капча).

## 🧪 5. Логика Агента (ScoutAgent)
- [ ] Оркестрация процесса:
    - [ ] Запрос в Ollama (планирование).
    - [ ] Вызов `ScraperClient` (исполнение).
    - [ ] Запрос в Ollama (анализ и саммари).
    - [ ] Сохранение в БД.

## ⏱ 6. Планировщик (Cron)
- [ ] Настройка `APScheduler` для периодического опроса БД и запуска агентов.

## ✅ 7. Тестирование
- [ ] Проверка связи между контейнерами (Backend -> Crawl4AI).
- [ ] Проверка скрапинга реального URL через внешний сервис.
